fastapi==0.115.0
uvicorn[standard]==0.30.0
python-dotenv==1.0.1
pydantic-settings==2.3.4
pytest==8.2.2
httpx==0.27.0
python-multipart==0.0.9
aiofiles==23.2.1
openai==1.40.0
faster-whisper==1.2.1
python-docx==1.1.2

# Local LLM (optional — only required if AI_PROVIDER=local)
# ollama==0.3.0

# RAG embeddings — local provider (required when EMBEDDING_PROVIDER=local)
sentence-transformers==3.0.1

# Enterprise Architect COM API (Windows only — NOT installed in Docker/Linux deploy)
# pip install -r requirements-windows.txt

# Lean deps for Render / production when TRANSCRIPTION_PROVIDER=groq|openai.
# Omits faster-whisper (heavy) and pytest (dev-only). Local dev uses requirements.txt.

fastapi==0.115.0
uvicorn[standard]==0.30.0
python-dotenv==1.0.1
pydantic-settings==2.3.4
certifi==2024.8.30
httpx==0.27.0
python-multipart==0.0.9
aiofiles==23.2.1
openai==1.40.0
python-docx==1.1.2

# RAG — local embeddings (CPU-only PyTorch wheel is much smaller than the default CUDA build)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0,<3.0.0
sentence-transformers==3.0.1
